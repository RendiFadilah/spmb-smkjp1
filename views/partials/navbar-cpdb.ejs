<nav class="bg-white border-b border-gray-100 fixed z-30 w-full shadow-lg transition-all duration-200 ease-in-out" id="navbar-cpdb">
    <div class="px-3 py-2 lg:px-5 lg:pl-3 max-w-7xl mx-auto">
        <div class="flex items-center justify-between h-12">
            <!-- Left side - Logo & Title -->
            <div class="flex items-center gap-2 lg:gap-3">
                <img src="/images/logo-jp1.png" class="h-7 w-auto" alt="SMK JT 1 Logo" />
                <div class="hidden md:block">
                    <span class="text-lg font-semibold text-gray-800">SPMB SMK Jakarta Pusat 1</span>
                </div>
                <!-- Mobile Title -->
                <span class="text-lg font-semibold text-gray-800 md:hidden">SPMB SMK Jakarta Pusat 1 1</span>
            </div>

            <!-- Center - Navigation Menu -->
            <div class="hidden md:flex items-center space-x-1">
                <a href="/dashboard/cpdb" class="nav-link px-3 py-2 text-sm font-medium text-gray-700 hover:text-green-600 rounded-md hover:bg-green-50 transition-all duration-200 <%= typeof currentPath !== 'undefined' && currentPath === '/dashboard/cpdb' ? 'bg-green-50 text-green-600 border border-green-100' : '' %>">
                    <i class="fas fa-home mr-2"></i>Beranda
                </a>
                <a href="/dashboard/cpdb/pendaftaran" class="nav-link px-3 py-2 text-sm font-medium text-gray-700 hover:text-green-600 rounded-md hover:bg-green-50 transition-all duration-200 <%= typeof currentPath !== 'undefined' && currentPath === '/dashboard/cpdb/pendaftaran' ? 'bg-green-50 text-green-600 border border-green-100' : '' %>">
                    <i class="fas fa-edit mr-2"></i>Pendaftaran
                </a>
            </div>

            <!-- Mobile Menu Button -->
            <div class="md:hidden">
                <button type="button" onclick="toggleMobileMenu()" class="inline-flex items-center justify-center px-3 py-1.5 rounded-md text-sm font-medium text-gray-700 hover:text-gray-900 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 transition-all duration-200 border border-gray-100">
                    Menu
                </button>
            </div>

            <!-- Right side - Profile -->
            <div class="flex items-center">
                <div class="relative">
                    <button type="button" 
                            onclick="toggleProfileMenu()"
                            class="group flex items-center gap-3 text-sm bg-white hover:bg-gray-50 rounded-full py-1.5 pl-1.5 pr-3 transition-all duration-200 border border-gray-100 hover:border-gray-200 hover:shadow-sm">
                        <div class="w-7 h-7 rounded-full bg-gradient-to-br from-green-500 to-green-600 flex items-center justify-center text-white font-medium text-sm shadow-inner">
                            <%= user.nama.charAt(0).toUpperCase() %>
                        </div>
                        <div class="text-left hidden sm:block pr-1">
                            <div class="text-sm font-medium text-gray-700 line-clamp-1 group-hover:text-gray-900"><%= user.nama || 'User' %></div>
                        </div>
                        <i class="fas fa-chevron-down text-gray-400 text-xs transition-transform duration-200 group-hover:text-gray-500" id="profile-chevron"></i>
                    </button>

                    <!-- Dropdown menu -->
                    <div class="hidden absolute right-0 mt-2 w-56 bg-white rounded-lg shadow-lg py-2 z-50 border border-gray-100 transform origin-top-right transition-all duration-200" id="profile-menu">
                        <!-- Header -->
                        <div class="px-4 py-3 border-b border-gray-100">
                            <div class="flex items-center gap-3">
                                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-green-500 to-green-600 flex items-center justify-center text-white font-medium shadow-inner">
                                    <%= user.nama.charAt(0).toUpperCase() %>
                                </div>
                                <div>
                                    <div class="text-sm font-semibold text-gray-900 line-clamp-1"><%= user.nama || 'User' %></div>
                                    <div class="text-xs text-gray-500 mt-0.5"><%= user.roles || 'Guest' %></div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Menu Items -->
                        <div class="py-1">
                            <a href="/dashboard/cpdb/settings" class="flex items-center px-4 py-2.5 text-sm text-gray-700 hover:bg-green-50 hover:text-green-600 transition-colors group <%= typeof currentPath !== 'undefined' && currentPath === '/dashboard/cpdb/settings' ? 'bg-green-50 text-green-600 border border-green-100' : '' %>">
                                <i class="fas fa-cog w-4 h-4 text-gray-400 mr-3 group-hover:text-green-600"></i>
                                <span class="group-hover:text-green-600">Pengaturan</span>
                            </a>
                            <hr class="my-1 border-gray-100">
                            <form action="/auth/logout" method="POST" class="w-full">
                                <button type="submit" class="flex items-center w-full px-4 py-2.5 text-sm text-red-600 hover:bg-red-50 transition-colors group">
                                    <i class="fas fa-sign-out-alt w-4 h-4 mr-3 group-hover:text-red-700"></i>
                                    <span class="group-hover:text-red-700">Keluar</span>
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Mobile menu panel -->
    <div class="hidden md:hidden bg-white border-b border-gray-100 shadow-lg transform origin-top transition-all duration-200" id="mobile-menu">
        <div class="px-2 pt-2 pb-3 space-y-1">
            <a href="/dashboard/cpdb" class="nav-link block px-3 py-2 text-base font-medium text-gray-700 hover:text-green-600 hover:bg-green-50 rounded-md transition-all duration-200 <%= typeof currentPath !== 'undefined' && currentPath === '/dashboard/cpdb' ? 'bg-green-50 text-green-600 border border-green-100' : '' %>">
                <i class="fas fa-home mr-2"></i>Beranda
            </a>
            <a href="/dashboard/cpdb/pendaftaran" class="nav-link block px-3 py-2 text-base font-medium text-gray-700 hover:text-green-600 hover:bg-green-50 rounded-md transition-all duration-200 <%= typeof currentPath !== 'undefined' && currentPath === '/dashboard/cpdb/pendaftaran' ? 'bg-green-50 text-green-600 border border-green-100' : '' %>">
                <i class="fas fa-edit mr-2"></i>Pendaftaran
            </a>
        </div>
    </div>
</nav>

<script>
function toggleMobileMenu() {
    const mobileMenu = document.getElementById('mobile-menu');
    if (mobileMenu.classList.contains('hidden')) {
        mobileMenu.classList.remove('hidden');
        mobileMenu.classList.add('animate-fadeIn');
    } else {
        mobileMenu.classList.add('hidden');
        mobileMenu.classList.remove('animate-fadeIn');
    }
}

function toggleProfileMenu() {
    const menu = document.getElementById('profile-menu');
    const chevron = document.getElementById('profile-chevron');
    
    if (menu.classList.contains('hidden')) {
        menu.classList.remove('hidden');
        chevron.style.transform = 'rotate(180deg)';
        // Add animation classes
        menu.classList.add('animate-fadeIn');
    } else {
        menu.classList.add('hidden');
        chevron.style.transform = 'rotate(0)';
        menu.classList.remove('animate-fadeIn');
    }
}

// Close dropdowns when clicking outside
document.addEventListener('click', (e) => {
    const menu = document.getElementById('profile-menu');
    const mobileMenu = document.getElementById('mobile-menu');
    const chevron = document.getElementById('profile-chevron');
    const button = e.target.closest('button');
    
    // Close profile menu if clicking outside
    if (!button && !menu.classList.contains('hidden')) {
        menu.classList.add('hidden');
        chevron.style.transform = 'rotate(0)';
    }
    
    // Close mobile menu if clicking outside
    if (!button && !mobileMenu.classList.contains('hidden')) {
        mobileMenu.classList.add('hidden');
    }
});

// Add styles for animation
const style = document.createElement('style');
style.textContent = `
    .animate-fadeIn {
        animation: fadeIn 0.2s ease-out;
    }
    
    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: scale(0.95) translateY(-0.5rem);
        }
        to {
            opacity: 1;
            transform: scale(1) translateY(0);
        }
    }
`;
document.head.appendChild(style);
</script>
