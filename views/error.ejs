<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - SPMB SMK Jakarta Pusat 1</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
        .error-container {
            animation: fadeInUp 0.5s ease-out;
        }
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center px-4">
    <div class="error-container text-center">
        <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full">
            <div class="mb-6">
                <i class="fas fa-exclamation-circle text-6xl text-red-500"></i>
            </div>
            <h1 class="text-2xl font-bold text-gray-800 mb-4">
                <%= error?.message || message || 'Terjadi Kesalahan' %>
            </h1>
            <p class="text-gray-600 mb-8">
                <% if (error?.status === 403) { %>
                    Anda tidak memiliki akses ke halaman ini.
                <% } else if (error?.status === 401) { %>
                    Sesi anda telah berakhir, silakan login kembali.
                <% } else { %>
                    Mohon maaf, terjadi kesalahan pada sistem.
                <% } %>
            </p>
            <div class="space-y-4">
                <button onclick="window.history.back()" class="w-full px-4 py-2 text-sm font-medium text-white bg-blue-600 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-colors">
                    <i class="fas fa-arrow-left mr-2"></i>Kembali
                </button>
                <a href="/" class="block w-full px-4 py-2 text-sm font-medium text-gray-700 bg-gray-100 rounded-lg hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-gray-300 transition-colors">
                    <i class="fas fa-home mr-2"></i>Halaman Utama
                </a>
            </div>
        </div>
    </div>
</body>
</html>
